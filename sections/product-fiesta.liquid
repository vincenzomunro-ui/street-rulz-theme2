{% comment %}
  Fiesta Product — Junior & Senior Fiesta checkout form.
  Fields: Team Name, Parent/Guardian Name, Child's Name, Child's Date of Birth, Email.
  All fields required. Parent registers on behalf of child (junior events).
{% endcomment %}
<div class="section">
  <h1 class="section-title">{{ product.title }}</h1>
  <div style="max-width:720px;font-size:16px;color:var(--gray-200);line-height:1.8;">
    {{ product.description }}
  </div>
  {% if product.available %}
    {% form 'product', product %}
      <input type="hidden" name="id" value="{{ product.selected_or_first_available_variant.id }}">

      <div style="margin-top:24px;max-width:480px;">
        {%- comment -%} Team Name — Required {%- endcomment -%}
        <div style="margin-bottom:16px;">
          <label for="team-name" style="display:block;font-weight:700;font-size:14px;color:var(--white);margin-bottom:6px;text-transform:uppercase;letter-spacing:0.5px;">
            Team Name <span style="color:var(--orange);">*</span>
          </label>
          <input
            type="text"
            id="team-name"
            name="properties[Team Name]"
            placeholder="e.g. The Strikers"
            required
            style="width:100%;padding:12px 16px;font-size:16px;border:2px solid var(--gray-600, #444);border-radius:8px;background:var(--gray-800, #1a1a1a);color:var(--white, #fff);outline:none;transition:border-color 0.2s;"
            onfocus="this.style.borderColor='var(--orange, #ff6600)'"
            onblur="this.style.borderColor='var(--gray-600, #444)'"
          >
          <p style="font-size:12px;color:var(--gray-400, #999);margin-top:4px;">
            Make sure all your teammates enter the same team name so we can group you together.
          </p>
        </div>

        {%- comment -%} Parent / Guardian Name — Required {%- endcomment -%}
        <div style="margin-bottom:16px;">
          <label for="parent-name" style="display:block;font-weight:700;font-size:14px;color:var(--white);margin-bottom:6px;text-transform:uppercase;letter-spacing:0.5px;">
            Parent / Guardian Name <span style="color:var(--orange);">*</span>
          </label>
          <input
            type="text"
            id="parent-name"
            name="properties[Parent Name]"
            placeholder="e.g. Sarah Smith"
            required
            style="width:100%;padding:12px 16px;font-size:16px;border:2px solid var(--gray-600, #444);border-radius:8px;background:var(--gray-800, #1a1a1a);color:var(--white, #fff);outline:none;transition:border-color 0.2s;"
            onfocus="this.style.borderColor='var(--orange, #ff6600)'"
            onblur="this.style.borderColor='var(--gray-600, #444)'"
          >
        </div>

        {%- comment -%} Child's Name — Required {%- endcomment -%}
        <div style="margin-bottom:16px;">
          <label for="child-name" style="display:block;font-weight:700;font-size:14px;color:var(--white);margin-bottom:6px;text-transform:uppercase;letter-spacing:0.5px;">
            Child's Name <span style="color:var(--orange);">*</span>
          </label>
          <input
            type="text"
            id="child-name"
            name="properties[Child Name]"
            placeholder="e.g. Jack Smith"
            required
            style="width:100%;padding:12px 16px;font-size:16px;border:2px solid var(--gray-600, #444);border-radius:8px;background:var(--gray-800, #1a1a1a);color:var(--white, #fff);outline:none;transition:border-color 0.2s;"
            onfocus="this.style.borderColor='var(--orange, #ff6600)'"
            onblur="this.style.borderColor='var(--gray-600, #444)'"
          >
        </div>

        {%- comment -%} Child's Date of Birth — Required {%- endcomment -%}
        <div style="margin-bottom:16px;">
          <label for="child-dob" style="display:block;font-weight:700;font-size:14px;color:var(--white);margin-bottom:6px;text-transform:uppercase;letter-spacing:0.5px;">
            Child's Date of Birth <span style="color:var(--orange);">*</span>
          </label>
          <input
            type="date"
            id="child-dob"
            name="properties[Child DOB]"
            required
            style="width:100%;padding:12px 16px;font-size:16px;border:2px solid var(--gray-600, #444);border-radius:8px;background:var(--gray-800, #1a1a1a);color:var(--white, #fff);outline:none;transition:border-color 0.2s;color-scheme:dark;"
            onfocus="this.style.borderColor='var(--orange, #ff6600)'"
            onblur="this.style.borderColor='var(--gray-600, #444)'"
          >
          <p style="font-size:12px;color:var(--gray-400, #999);margin-top:4px;">
            Used to verify your child is in the correct age group.
          </p>
        </div>

        {%- comment -%} Email Address — Required {%- endcomment -%}
        <div style="margin-bottom:24px;">
          <label for="customer-email" style="display:block;font-weight:700;font-size:14px;color:var(--white);margin-bottom:6px;text-transform:uppercase;letter-spacing:0.5px;">
            Email Address <span style="color:var(--orange);">*</span>
          </label>
          <input
            type="email"
            id="customer-email"
            name="properties[Email]"
            placeholder="e.g. sarah@example.com"
            required
            style="width:100%;padding:12px 16px;font-size:16px;border:2px solid var(--gray-600, #444);border-radius:8px;background:var(--gray-800, #1a1a1a);color:var(--white, #fff);outline:none;transition:border-color 0.2s;"
            onfocus="this.style.borderColor='var(--orange, #ff6600)'"
            onblur="this.style.borderColor='var(--gray-600, #444)'"
          >
          <p style="font-size:12px;color:var(--gray-400, #999);margin-top:4px;">
            We'll send event updates, draw info, and confirmation to this email.
          </p>
        </div>
      </div>

      <button type="submit" class="btn-primary" style="margin-top:8px;">
        Add to Cart — {{ product.price | money }}
      </button>
    {% endform %}
  {% else %}
    <p style="margin-top:24px;font-size:18px;color:var(--orange);font-weight:700;">Sold Out</p>
  {% endif %}
</div>

{% schema %}
{
  "name": "Fiesta Product",
  "settings": []
}
{% endschema %}
